# この処理系の進め方

作業のたびに読む覚え書き。<b>何を作るか</b>は要件定義書と設計に書いてある。
ここに書くのは<b>どう進めるか</b>である。

大きな目標 (メインフレームの振る舞いを忠実に再現する) には、一息では届かない。
届くのは<b>測って、直して、また測る</b>を回し続けたときだけである。以下はその回し方で
実際に効いたものだけを残している。

---

## 1. 外の基準を先に置く

自分で書いた期待値だけで緑にしても、<b>正しさの裏付けにはならない</b>。
機能を足す前に、外から測る道具を作る。

- 命令レベル: Hercules (`cobol-oracle`)
- 言語レベル: NIST CCVS85 (`cobol-verify`)

道具を作った当日、<b>自分では思い付かなかった</b>不備が 8 つ出た。以後、増分の順番は
ほぼすべてこの道具が決めている。

## 2. 直すたびに測る

門を 1 つ開けると、<b>その奥にあったものが数に出る</b>。1 度測って終わりにしない。

相対添字を実装した次の測定で、<b>自分がその増分で入れた不具合</b>が 4 本の塊として
出た。自分のテストは間違ったほうの規則を固定していたので、緑のままだった。
次の測定が無ければ、資産を動かすまで見つからなかった。

## 3. 数の一覧に自分の診断が並んだら、まずそれを疑う

このセッションで 5 回あった。どれも<b>規格が許している書き方を、こちらが狭く決めて
いた</b>ものである。

| 自分の診断 | 実際は |
| --- | --- |
| `SET requires an index name` | `SET ... TO` の受取側は整数の項目でもよい |
| `an arithmetic statement requires a numeric receiver` | `GIVING` の受取側は数字編集項目でよい |
| `SEARCH on a table inside another table is not supported yet` | 内側の指標を動かすだけなので、できる |
| `X requires n subscript(s) but n were given` | 自分が入れた不具合だった |

そして毎回、<b>自分のテストが狭いほうを正しいこととして固定していた</b>。

> **自分で書いた規則は、自分のテストでは破れない。**

外の基準を置く理由はここにある。

## 4. 理由が細かくなったら、数えるのをやめて行を見る

理由の文面が違っても、原因は同じことがある。診断は位置を持っているので、
<b>止まった原文の行</b>を引けばよい。

```
IF119A  COMPUTE WS-NUM = FUNCTION MAX(IND(ALL)).
IF120A  COMPUTE WS-NUM = FUNCTION MEAN(IND(ALL)).
IF121A  COMPUTE WS-NUM = FUNCTION MEDIAN(IND(ALL)).
```

11 本が同じ `ALL` で止まっていたのに、関数名が違うので 1 件ずつに散っていた。
段分けと `ALTER` も同じ形で埋もれていた。<b>3 度あった。</b>

## 5. 「仕様が無いから書けない」を定期的に読み直す

`SQRT` や `SIN` を「参照実装の桁数の規則を持っていないから書かない」と決めていた。
根拠に置いたのは「COBOL の資産は計算結果を等号で比べる」という<b>自分の思い込み</b>で
ある。検査スイートを読んだら、範囲で判定していた。

```
EVALUATE FUNCTION ANNUITY(2.9, 4)
WHEN  2.91252 THRU  2.91264   PERFORM PASS
```

<b>適合性の検査スイートは、規格の要求を実行できる形で書いたものである。</b>
数を測る道具であるだけではない。「仕様が無い」と自分で決めたところは、まず読む。
この 1 つで 13 本が動いた。

## 6. 検査の道具が、処理系の失敗を作ってはならない

道具が作った失敗を処理系の失敗と読み違えると、<b>数そのものが信用できなくなる</b>。

- 差し込み札 (X-card) を埋めていないプログラムは<b>流さない</b>
- 試験のソースは 72 桁で切れる。超えたら<b>試験のほうを落とす</b> (`FixedFormatSource`)
- 診断の引用は<b>尻を残す</b>。頭は段落名から始まるので、同じ原因が散る

3 度やられた。<b>道具にも検査を仕込む。</b>

裏返しも要る。<b>処理系の失敗が、測定を止めてはならない。</b>1 本が返ってこないだけで
残り全部の数が消える。翻訳には時間の限りを置き、超えたら見捨てて<b>壊れた</b>と数える
(`CorpusRunner.LIMIT_SECONDS`)。返ってこない翻訳系は、断る翻訳系より悪い。

## 7. 数を門にしない

合格率に下限を課すと、処理系が育つまでビルドが赤くなり、<b>測り続けられなくなる</b>。
数は記録して並べるものであって、通す・落とすの門にするものではない。
門になるのは要件 13 章の受け入れ基準だけであり、それは人が見る。

## 8. 暫定判断は書き、そして読み直す

先送りした判断は `docs/decisions/provisional.md` に<b>解消条件つき</b>で残す。

<b>書いた暫定判断そのものが間違っていることがある。</b>P-065 (関数の桁数) と
P-066 (段分け) は、あとで根拠ごと書き直した。記録は「もう考えなくてよい印」ではなく
<b>「あとで疑うための印」</b>である。

---

## 決めごと

| 項目 | 内容 |
| --- | --- |
| 言語 | ドキュメント・コメント・コミットメッセージは日本語 |
| ブランチ | `claude/jvm-cobol-requirements-1phhbu` |
| ビルド | `HERCULES=/usr/bin/hercules mvn -B -o clean test` |
| CCVS85 | `java -cp <classes> dev.cobolonjava.verify.Main ccvs85 <newcob.val>` |
| コーパス | <b>同梱しない</b>。URL と sha256 で固定して取得する (要件 NFR-042) |
| Hercules | QPL であり Apache-2.0 と両立しない。<b>外から呼ぶだけ</b>。ソースを読んで写さない |
| 出典 | IBM 製品のソースは参照しない。公開仕様と観測できる振る舞いだけを根拠にする |

コメントは<b>なぜそうしたか</b>を書く。何をしているかはコードが語る。
「ここは規格がこう決めている」「実機と突き合わせていない」は、必ず書き残す。

## 増分の型

1. 測る (CCVS85 を流す)
2. いちばん大きい塊を選ぶ。理由が細かければ<b>止まった行</b>を見る
3. 実装する。<b>近い値を黙って返すくらいなら、書けないと断る</b>
4. 振る舞いの差が出る形でテストを書く (`CONTINUE` と `NEXT SENTENCE` を区別する等)
5. 全テストを流し、測り直す
6. 設計と暫定判断を更新し、数を README に書く
7. 1 つの増分 = 1 コミット。コミットメッセージに<b>何が分かったか</b>を書く

この 7 段を回している限り、進み続けられる。1 回の増分で解けない問題は、
<b>次の測定が見つけてくれる</b>。
